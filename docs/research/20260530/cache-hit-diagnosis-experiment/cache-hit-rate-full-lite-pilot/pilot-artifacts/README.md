# Cache-Hit Pilot Artifacts

This directory stores small, reviewable artifacts produced by the eight-pair
Full/Lite cache-hit pilot.

The planning/setup session must not be recorded as a pilot row. Only dedicated
run sessions that start with `CACHE_HIT_EXPERIMENT_RUN` should create files
here and later be recorded in `experiment-runs.csv` by the controller.
